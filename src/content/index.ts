/**
 * Optional content script placeholder for future read-only page context helpers.
 * Currently unused in manifest — kept for extension architecture completeness.
 * Must never scrape restricted pages or bypass platform protections.
 */
export function noopContentScript() {
  return true;
}
