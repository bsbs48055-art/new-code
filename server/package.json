{
  "name": "sms-pro-helper-server",
  "version": "1.0.0",
  "description": "Optional local helper server for Social Media Studio Pro. Performs the single OAuth steps (TikTok code→token exchange, Facebook long-lived token exchange) that require a confidential client secret, so that secret never has to live inside the Chrome extension bundle.",
  "private": true,
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
